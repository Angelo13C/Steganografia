from setuptools import setup

setup(
   name='steganography',
   version='1.0',
   description='Hide communication',
   author='Angelo Cipriani',
   install_requires=['pyperclip', 'PyQt5'] #external packages as dependencies
)